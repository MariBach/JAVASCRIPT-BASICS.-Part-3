// Write a JavaScript function to get the last element of an array

let num_array = [1, 3, 5, 6, 2];
function last_element(arr) {
    last_elem = arr.pop();
    console.log(last_elem);
}
last_element(num_array);

